{
  "buildCommand": "npx prisma generate && (test -n \"$DATABASE_URL\" && npx prisma db push --accept-data-loss || true) && npx next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
