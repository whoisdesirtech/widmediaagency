{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
